apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vcluster-controller
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "4"
spec:
  project: control-plane
  source:
    repoURL: file:///repo
    targetRevision: HEAD
    path: control-plane/gitops/argocd/main/apps/infrastructure/vcluster
  destination:
    server: https://kubernetes.default.svc
    namespace: vcluster-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
