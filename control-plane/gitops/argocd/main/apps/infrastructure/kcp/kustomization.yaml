apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kcp

resources:
  - certificate.yaml
  - kcp-oidc-ca-external-secret.yaml
  - kcp-secret.yaml
  - kcp-postgres-secret.yaml
  - kine.yaml
  - service.yaml
  - deployment.yaml
  - syncagent-kubeconfig-external-secret.yaml
  - syncagent-rbac-agencies.yaml
  - syncagent-release.yaml

commonLabels:
  app.kubernetes.io/part-of: euroscale
  app.kubernetes.io/managed-by: argocd
