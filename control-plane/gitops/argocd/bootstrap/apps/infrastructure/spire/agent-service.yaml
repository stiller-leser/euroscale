apiVersion: v1
kind: Service
metadata:
  name: spire-agent
  namespace: spire-system
spec:
  type: ClusterIP
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
      name: agent-api
  selector:
    app: spire-agent
