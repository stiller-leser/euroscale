apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cloudnative-pg-resources
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  project: control-plane
  source:
    repoURL: file:///repo
    targetRevision: HEAD
    path: control-plane/gitops/argocd/bootstrap/apps/database/cloudnative-pg-resources
  destination:
    server: https://kubernetes.default.svc
    namespace: cnpg
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      limit: 12
      backoff:
        duration: 20s
        factor: 2
        maxDuration: 5m
    syncOptions:
    - CreateNamespace=true
